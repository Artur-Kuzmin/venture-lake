// Phase 2 implements the global queue + party panel on the lobby.
export default function LobbyPage() {
  return (
    <div className="page">
      <h1>Lobby</h1>
      <p className="placeholder">Join the global queue, solo or with a party. (Coming in Phase 2.)</p>
    </div>
  );
}
