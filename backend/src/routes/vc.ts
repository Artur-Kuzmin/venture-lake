import { Router } from 'express';

// VC reviewer routes. Implemented across Phase 7.
// Endpoints (later): GET /me, queue/enter, assignments/:id/accept|pass|review,
//                    current-assignment.
const router = Router();

export default router;
