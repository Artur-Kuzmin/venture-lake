import { Router } from 'express';

// Public showcase routes. Implemented across Phase 10.
// Endpoints (later): publish flow + public project listing.
const router = Router();

export default router;
