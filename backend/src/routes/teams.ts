import { Router } from 'express';

// Team lobby + mission flow routes. Implemented across Phases 4–5.
// Endpoints (later): GET /:id, messages, ready, leave, idea generation/voting,
//                    captain nomination/vote, deliverables.
const router = Router();

export default router;
