import { Router } from 'express';

// Party queue routes. Implemented in Phase 2.2.
// Endpoints: POST /, POST /:id/invite, POST /:id/join, POST /:id/leave,
//            POST /:id/queue, GET /me
const router = Router();

export default router;
