import { Router } from 'express';

// Admin routes (VC approval, monitoring, exceptions). Implemented in Phase 7.1+.
// Endpoints (later): POST /users/:id/approve-vc, POST /users/:id/revoke-vc.
const router = Router();

export default router;
