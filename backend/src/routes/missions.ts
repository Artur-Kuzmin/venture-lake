import { Router } from 'express';

// Mission routes (start, submit, deliverable assignments). Implemented across
// Phases 5–6. Endpoints (later): POST /:id/submit, PUT /:id/deliverable-assignments.
const router = Router();

export default router;
