import { Router } from 'express';

// Global queue routes (solo). Implemented in Phase 2.1.
// Endpoints: POST /join, POST /leave, GET /me, GET /status
const router = Router();

export default router;
